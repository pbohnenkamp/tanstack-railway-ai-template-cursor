# Todos (drizzle demo) — e2e

Browser journey for the public `/demo/drizzle` page. Uses the long-lived Compose
`app` database via the running app (not `app_test`).

## Scenarios

- Visitor can add a todo and see it listed
