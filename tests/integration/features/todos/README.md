# Todos (drizzle demo)

Public demo feature: list and create todos via Drizzle handlers (no `authed` /
owner column — intentional for the template demo; product resources must differ).

## Scenarios

- List returns rows newest-first after inserts
- Create persists a title and surfaces it in the list
